shots/*.png
assets/cut/*.png
assets/cut/*.jpg
shots/mob.html
shots/.mob-*.html

# recovered source archive, kept local for the extractor
.source/
shots/*.log

# Claude Code local permission grants — machine-specific, not shared
.claude/settings.json
.claude/settings.local.json

# Vercel CLI build output, written into the deploy tree by `vercel deploy`
site/.vercel/
